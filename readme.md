# Lottery

## Installation

Via Composer

``` bash
$ composer install
$ php artisan key:generate
$ php artisan migrate --seed
$ npm i
$ npm run prod
```

##unit test 

``` bash
$ vendor/bin/phpunit
```